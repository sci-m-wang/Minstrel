interface:
  display_name: "SideProfile Experiment"
  short_description: "Run a frozen SideProfile bundle on GPU"
  default_prompt: "Use $side-profile-experiment to verify and run the frozen core experiment unchanged, then attempt each available official external baseline without letting baseline failures block the core. Apply official evaluators and produce an evidence-backed report."
policy:
  allow_implicit_invocation: true
